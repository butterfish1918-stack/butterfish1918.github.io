window.__GRIMOIRE_CODE = (window.__GRIMOIRE_CODE || "") + "argetNotes.includes(STRINGS_MIDI[s] % 12))\n                                opts.push(0);\n                            let searchMin = Math.max(1, rf - 2);\n                            let searchMax = Math.min(22, rf + 3);\n                            for (let f = searchMin; f <= searchMax; f++) {\n                                if (targetNotes.includes((STRINGS_MIDI[s] + f) % 12))\n                                    opts.push(f);\n                            }\n                            stringOptions.push(opts);\n                        }\n                    }\n                    for (let v0 of stringOptions[0]) {\n                        for (let v1 of stringOptions[1]) {\n                            for (let v2 of stringOptions[2]) {\n                                for (let v3 of stringOptions[3]) {\n                                    for (let v4 of stringOptions[4]) {\n                                        for (let v5 of stringOptions[5]) {\n                                            let voicing = [v0, v1, v2, v3, v4, v5];\n                                            let ringingCount = 0, openCount = 0, frets = [], foundNotes = new Set();\n                                            for (let i = 0; i < 6; i++) {\n                                                let v = voicing[i];\n                                                if (v !== 'X') {\n                                                    ringingCount++;\n                                                    foundNotes.add((STRINGS_MIDI[i] + v) % 12);\n                                                    if (v === 0)\n                                                        openCount++;\n                                                    else\n                                                        frets.push(v);\n                                                }\n                                            }\n                                            if (!targetNotes.every(n => foundNotes.has(n)))\n                                                continue;\n                                            let span = frets.length > 0 ? Math.max(...frets) - Math.min(...frets) : 0;\n                                            if (span > 3)\n                                                continue;\n                                            let uniqueFretsCount = new Set(frets).size;\n                                            let minFret = frets.length > 0 ? Math.min(...frets) : 0;\n                                            let minFretOccurrences = frets.filter(f => f === minFret).length;\n                                            let fingersRequired = uniqueFretsCount;\n                                            if (minFretOccurrences >= 2)\n                                                fingersRequired -= (minFretOccurrences - 1);\n                                            if (fingersRequired > 4)\n                                                continue;\n                                            let score = (ringingCount * 10) + (openCount * 8) - (fingersRequired * 10) - (span * 5) - minFret;\n                                            if (minFretOccurrences >= 3)\n                                                score += 20;\n                                            if (score > bestScore) {\n                                                bestScore = score;\n                                                bestVoicing = voicing;\n                                            }\n                                        }\n                                    }\n                                }\n                            }\n                        }\n                    }\n                }\n            }\n            bestVoicingFound = bestVoicing || ['X', 'X', 'X', 'X', 'X', 'X'];\n            if (!bestVoicing)\n                bestVoicingFound[5] = (chordRoot - (STRINGS_MIDI[5] % 12) + 12) % 12;\n        }\n        let tabStr = \"\";\n        let noteStr = \"\";\n        let midiPitches = [];\n        for (let i = 0; i < 6; i++) {\n            let tLine = `${STRING_NAMES[i]} |---`;\n            let nLine = `${STRING_NAMES[i]} |---`;\n            let v = bestVoicingFound[i];\n            if (v === 'X') {\n                tLine += \"X---|\\n\";\n                nLine += \"X---|\\n\";\n            }\n            else {\n                tLine += (v < 10 ? `${v}---|\\n` : `${v}--|\\n`);\n                const absoluteMidi = STRINGS_MIDI[i] + v;\n                midiPitches.push(absoluteMidi);\n                const nName = NOTES[absoluteMidi % 12];\n                nLine += (nName.length === 1 ? `${nName}---|\\n` : `${nName}--|\\n`);\n            }\n            tabStr += tLine;\n            noteStr += nLine;\n        }\n        return { tabStr, noteStr, midiPitches: midiPitches.reverse() };\n    }, [safeActiveDegree, activeChords]);\n    return (React.createElement(\"div\", { className: \"min-h-screen bg-[#0A0A0A] text-[#D1C6B4] p-2 sm:p-4 md:p-8 relative font-sans select-none overflow-x-hidden\", style: { backgroundImage: 'radial-gradient(circle at 50% 50%, #1a1616 0%, #050505 100%)' } },\n        React.createElement(\"div\", { className: \"pointer-events-none absolute inset-0 z-50 opacity-20\", style: { backgroundImage: `url(\"data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.8' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E\")` } }),\n        React.createElement(\"div\", { className: \"pointer-events-none absolute inset-0 z-40 mix-blend-overlay opacity-30\", style: { background: 'linear-gradient(rgba(18, 16, 16, 0) 50%, rgba(0, 0, 0, 0.25) 50%), linear-gradient(90deg, rgba(255, 0, 0, 0.06), rgba(0, 255, 0, 0.02), rgba(0, 0, 255, 0.06))', backgroundSize: '100% 4px, 3px 100%' } }),\n        React.createElement(\"div\", { className: \"w-full max-w-6xl mx-auto relative z-10 flex flex-col gap-6 sm:gap-8\" },\n            React.createElement(\"div\", { className: \"flex flex-col md:flex-row justify-between items-center border-b border-[#332] pb-6\" },\n                React.createElement(\"div\", { className: \"text-center md:text-left\" },\n                    React.createElement(\"h1\", { className: \"text-3xl sm:text-4xl md:text-5xl tracking-widest text-[#DC143C]\", style: { fontFamily: \"'Cinzel Decorative', cursive\", textShadow: '0 0 15px rgba(220,20,60,0.5)' } }, \"THE GRIMOIRE ENGINE\"),\n                    React.createElement(\"p\", { className: \"text-xs sm:text-sm tracking-[0.2em] opacity-60 mt-2 font-mono\" }, \"Modulo-12 Fretboard Abstraction\")),\n                React.createElement(\"div\", { className: \"flex flex-col gap-4 mt-6 md:mt-0 font-mono text-xs sm:text-sm w-full md:w-auto\" },\n                    React.createElement(\"div\", { className: \"flex flex-wrap justify-center gap-2 mb-2 border-b border-[#332] pb-4\" }, ['EXPLORE', 'PRACTISE', 'QUIZ', 'LIVE'].map(mode => (React.createElement(\"button\", { key: mode, onClick: () => { setAppMode(mode); if (['QUIZ', 'PRACTISE'].includes(mode))\n                            initAudio();";
